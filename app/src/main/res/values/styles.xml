<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FugasApp" parent="android:Theme.Light.NoActionBar">
        <!-- Solo para cumplir con el Manifest, el tema real es FugasAppTheme en Compose -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <!-- Alias para compatibilidad si alguna dependencia lo requiere -->
    <style name="Theme.Material3.DayNight.NoActionBar" parent="android:Theme.Light.NoActionBar" />
</resources>
